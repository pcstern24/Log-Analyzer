# Log-Analyzer
Java-based security log analyzer that parses server logs, tracks login activity, and detects suspicious behavior such as repeated failed login attempts and potential brute-force attacks.
